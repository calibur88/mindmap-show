---
mms_name: 技术架构
mms_tags: [示例, 综合演示]
mms_layout: TB
mms_line: curve
mms_desc: 服务拆分与治理，含跨文件引用演示（连线走曲线）
---

# 技术架构

分层设计，接入层只做鉴权与路由。

** 整体原则待评审组确认；本期先按最小可用方案推进

## 接入层

** 这里有一点争议：是否走统一网关还是各业务侧自管路由，待架构会拍板

### API 网关

##### 限流熔断

从 depth 2 直接跳到 depth 4，中间缺失的一层由解析器自动补空节点。

** 跳级提示由 parser 自动补空节点；UI 上显示「（自动补齐）」

## 服务层

-- 用户服务

<=> 综合演示/用户增长脑图.mms 社交裂变    跨文件引用示例

** 这个跨文件引用会画一条虚线连接另一个脑图里的节点；目标文件有两个同名「社交裂变」，按规则指向第一个（渠道策略下），调试信息区会记一条 info 说明落点

-- 订单服务

## 数据层

### 主库

#### 只读副本

##### 分片 01

###### 分片 02

####### 分片 03

######## 分片 04

超过 H6 的层级，验证嵌套无上限。

** 分片 04 是最深层；继续往下写依然合法（depth 8+）

## 架构图

![[详细分析.mms]]
