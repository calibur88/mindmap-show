---
mms_name: 指令基础
mms_tags: [示例, 指令扩展]
mms_layout: LR
mms_desc: `!--` 指令的绑定与解析机制：默认绑定、目标绑定、key 归一化、行尾注释剥离、同 key 冲突
---

!-- background > #FDF6E3 <**首个节点声明之前的是孤儿指令，挂文档级（渲染时作全文档兜底）
!-- text color > #2C3E50 <**带空格的 key 归一化为 text-color

# 产品路线图
** 对照组：自身只挂一条指令，其余效果全部来自继承与文档级兜底
** 米白底（#FDF6E3）与深蓝灰字（#2C3E50）来自上面的两条孤儿指令

!-- color > #C0392B <**默认绑定：写入上方最近的节点声明行（产品路线图）
!-- color > #E67E22 <**同 key 重复：保留先写者，本条记 directive-conflict 警告
** 文字色＝砖红（#C0392B）；指令块穿插注释行、空行均不打断绑定

## 稳定性
** 连线色＝淡紫蓝（#95A5F6）；文字色沿祖先链继承砖红

!-- line color > #95A5F6 <**行尾注释之后的内容整段丢弃；带空格 key 归一化为 line-color
### 事故复盘
** 自身无指令：连线就近继承「稳定性」，文字色继承「产品路线图」，底色兜底自文档级

!-- color > #E67E22 <-- ### 性能基线
### 性能基线
** 目标绑定（前向引用）：指令写在节点上方，用 <-- 指名绑给本节点；文字色＝橙（#E67E22），就近覆盖祖先链的砖红

## 易用性
** 对照组：零指令，默认深度配色，可与上方分支对比

-- 新手引导
** `--` 子节点行同样可作为默认绑定的落点
-- 操作手册
** 同上
