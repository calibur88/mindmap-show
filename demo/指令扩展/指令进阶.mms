---
mms_name: 指令进阶
mms_tags: [示例, 指令扩展]
mms_layout: LR
mms_desc: 全部 10 个标准 key 的效果全景：7 个样式类 + 3 个行为类，逐节点备注说明
---

# 指令效果全景
** 对照组：根节点不挂指令；每个分支演示一个 key，先记住这里的默认样子（深度配色／默认连线）

## color 文字色
!-- color > #E74C3C
** color＝文字／前景色（#E74C3C＝正红）：探索与全景两视图一致，均染文字不染底色
### 红字的子节点
** 自身无指令：color 沿祖先链就近继承，文字同为正红

## text-color 文字色
!-- text-color > #8E44AD
** text-color＝纯文字色（#8E44AD＝紫色）：与 color 同落文字色，声明在后者优先（同时声明时本 key 胜出）
### 紫字的子节点
** text-color 也是样式类：子节点沿链补齐，文字同紫

## background 背景色
!-- background > #F1C40F
** background＝节点背景（#F1C40F＝明黄）：DOM 落 background-color，SVG 映射为矩形 fill
### 黄底的子节点
** 背景沿祖先链继承：本节点自身无指令，底色来自父级

## border-radius 圆角
!-- border-radius > 16
** border-radius＝圆角半径 16px（对照其他分支的默认小圆角）；SVG 落矩形 rx
### 更圆的子节点
** 圆角继承：子节点同样变圆

## opacity 透明度
!-- opacity > 0.4
** opacity＝不透明度 0.4（40%）：整个节点半透明，可透出画布底色；超界值自动收敛到 [0,1]
### 半透明的子节点
** 透明度继承：本分支整棵子树都是 40%

## line-color 连线色
!-- line-color > #3498DB
** line-color＝连线颜色（#3498DB＝湖蓝）：连到本节点的线与整条子树连线一起变蓝（树边取子端样式）
### 蓝线子节点
** 连线样式沿链继承：父级声明一次，整条分支连线同色

## line-width 连线粗细
!-- line-width > 3
** line-width＝连线粗细 3（探索视图默认 2）；与 line-color 相互独立、可同时声明
### 粗线子节点
** 粗细继承自父级：本分支连线比默认粗

## collapsed 初始折叠
!-- collapsed
** collapsed＝初始折叠：徽标显示 +2（隐藏 2 个子节点）；点击展开/收起直接写回文档的这一行指令，持久化即文档
### 折叠期间不可见 A
** 折叠期间不参与布局，展开后恢复
### 折叠期间不可见 B
** 徽标 +2 的计数＝A 与本节点两个后代；展开后上方 collapsed 行即被删除

## debug 调试叠加
!-- debug
** debug＝调试叠加层：节点左上角灰底小签显示「完整节点 id · 源码行号」，用于排查指令绑到了谁
### 无叠加的子节点
** debug 是行为类：不继承，仅绑定的节点显示叠加层

## locked 锁定
!-- locked
** locked＝锁定：本节点禁点击选中（光标变禁用圆圈、悬停提示已锁定），右栏「节点名称」值置灰
### 未锁的子节点
** locked 不继承：子节点不受影响，可正常交互
