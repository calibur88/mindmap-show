# 项目文档通用生成模板

> 本文档是项目文档体系的**权威生成模板**，适用于 README、ARCHITECTURE、CONTRIBUTING、CHANGELOG 及示例库 README 的编写与校验。  
> 当前模板版本：1.1 · 所有派生文档必须遵守本文的通用约束和分类定位。

---

## 1. 跨文档通用约束（所有文档必须遵守）

| 约束类别 | 具体规则 |
|---|---|
| **身份声明** | 权威性文档（架构/规范类）头部必须声明“本文是 XX 的权威说明”+ 当前版本号，让读者知道以谁为准 |
| **语言与排版** | 正文用中文；代码、路径、键名用 ASCII 直引号；中文与英文/数字之间不加空格；代码块标注语言类型 |
| **条目可验证性** | 每条变更/规则的描述必须让读者“能据此验收”，不写空话 |
| **禁止事项** | ① 禁止过程性叙事（重构说明/迁移说明/优化说明）<br>② 禁止变更日志式注释 <br>③ **个人署名规则**：AI 生成内容默认禁止添加个人署名；仅在编写协议文件（如 LICENSE）或项目固有字段（如 AUTHORS）明确要求时方可保留。**严禁 AI 擅自修改、删除或替换项目中已有的任何个人/组织署名**。 >>> <br>④ 禁止编造或删除历史条目 <br>⑤ 禁止只改源码不改文档 |
| **同步链条** | 任何功能/语法/版本变更，必须同步全部相关文档（用“文件 × 需更新内容”表格明确职责） |
| **格式范式** | 结构性内容用目录树/ASCII 图/箭头链；对比性内容用表格；操作流程用 bash 代码块 + 编号步骤；警示用引用块 `>`；破坏性变更必须显式标注 |
| **粒度控制** | 变更条目用 `**加粗标题**：一句话核心价值` 开头 + `-` 列表展开；不宜过粗也不宜过碎 |
| **测试与兼容声明** | 涉及版本变更的条目，末尾必须有测试通过情况和版本兼容声明 |

---

## 2. 五类文档的定位分工

```
README.md          → 60 秒看懂：是什么、怎么装、怎么用（面向使用者）
ARCHITECTURE.md    → 深入理解：分层、依赖、数据流（面向开发者）
CONTRIBUTING.md    → 怎么参与：流程、规范、红线（面向协作者/AI）
CHANGELOG.md       → 演进史：每个版本改了什么、破坏了什么（面向升级者）
示例库 README.md    → 验收手册：在真实环境逐项验证功能（面向验收者）
```

---

## 3. 通用生成 Prompt（骨架）

```markdown
你是文档工程师。请按以下规范为项目【项目名】生成文档。遵守通用约束：
【通用约束】
1. 正文中文，代码/路径用 ASCII 直引号，中英文间不加空格；
2. 结构性内容用树状图，对比用表格，流程用代码块+编号，警示用 `>` 引用块；
3. 每个条目必须可验证、可验收；
4. 禁止：过程性叙事、违规署名、编造、只改代码不改文档（详见署名规则）；
5. 版本号三处同步（manifest/package/versions 类文件），文档头部标当前版本；
6. 破坏性变更必须在条目末尾用 **破坏性** 标注，大版本在条目开头用 `> **破坏性大版本**` 警示。
【要生成的文档】：__从 2.2~2.6 中选取对应模板__
【项目信息】：__填入技术栈、目录结构、核心概念、当前版本__
```

---

## 4. 各文档模板

### 4.1 README.md（项目主页）

**定位**：新用户 60 秒内看懂项目全貌。

```markdown
# 项目名
一句话定位（是什么 + 核心能力）
- **特性1关键词**：一句话说明；
- **特性2关键词**：一句话说明；
- **特性3关键词**：一句话说明。
> **版本兼容性**：当前版本与上一版本是否兼容、是否涉及语法变更、已有配置可否沿用。
## 安装
（放置文件路径 / 依赖要求，步骤化）
## 快速开始
```bash
安装 → 构建 → 测试 → 运行 的命令序列，每条命令附一句注释说明产物去向
```
## 核心功能
1. **功能名**：一句话 + 关键细节（存哪里、怎么触发、什么行为）；
2. ...（编号列表，每条 1~2 行）
## XX 快速上手
（核心语言/配置的 3~5 行最小示例 + 关键词速查表）
完整语法见 [规范文档链接]。
## 文档索引
| 文档 | 说明 |   ← 表格，列出全部兄弟文档及职责
## License
许可类型 + 特殊限制 + 联系方式。
```

**编写规则**：
- 首段必须是“一句话 + 特性列表”，不放背景故事；
- 版本信息（版本号 / ID / 最低依赖 / 语言版本）集中用列表声明；
- 特性与功能分层：特性是“卖点”，核心功能是“操作面”；
- 目录树放在“工程结构”章节，树内用中文注释标注每个目录职责。

---

### 4.2 ARCHITECTURE.md（架构文档）

**定位**：工程的**权威**架构说明。

```markdown
# 项目整体架构
> 本文是工程的权威架构说明。当前版本：X.Y.Z · 语言版本 N.M · 最低依赖 V。
## 1. 项目定位
1. **层1名称**：做什么 + 设计原则；
2. **层2名称**：做什么 + 设计原则；
3. **层3名称**：做什么 + 设计原则。
（核心设计原则单独列出：每条一个加粗短语 + 冒号 + 解释）
## 2. 工程结构
（目录树，树内注释说明用途）
| 目录 | 用途 | 是否入库 |   ← 易混淆目录用三列表格对比
|---|---|---|
## 3. 代码架构
（模块树：入口 → 各层 → 层内文件及职责）
分层依赖（ASCII 图，标注单向依赖与禁令）：
```
main ──► ui ──► dsql/
  └────► index/ ─┘
```
- 每层的架构约束（零依赖、唯一类型出口、纯函数等）逐条列出；
- 约定俗成的路径别名/命名规则在此声明。
### 数据流
```
事件A → 组件B → 组件C → 组件D → 渲染
```
（一行链路讲清从输入到输出的完整路径；配置/状态存哪、如何同步单独说明）
## 4. 构建与测试
```bash
命令序列
```
- 构建流程：入口 → 产物 → 自动同步到哪；
- 测试流程：运行器机制、套件注册方式；
- 测试套件表格：| 套件 | 领域 | 例数 |
## 5. 文档索引
| 文档 | 说明 |
## 6. 现状
**已实现**（版本 / 语言版本）：逗号分隔列举已落地能力；
**规划**：统一指向 TODO 文件（无则说明已清空）。
```

**编写规则**：
- 必须以“权威声明 + 版本头”开头；
- 先原则后细节：定位 → 结构 → 代码 → 流程 → 现状，由宏观到微观；
- 依赖关系**必须画图**且标注“单向，不得反向依赖”；
- 易混淆概念（如两个测试库）用表格对比而非散文描述；
- “现状”章是架构文档的收口：已实现 / 规划 分列。

---

### 4.3 CONTRIBUTING.md（开发规范）

**定位**：开发、版本、文档、提交的**统一规范**，与 README/ARCHITECTURE 配合阅读。

```markdown
# 开发与更新规范
> 本文是项目的开发、版本、文档与 git 提交的统一规范，与 README（总说明）、
> ARCHITECTURE（架构）配合阅读。
## 1. 文档地图
| 文档 | 职责 |   ← 全部文档一览，明确本文管的范围
## 2. 工程结构约定
- 条目式列出硬性约定（目录职责、什么入库什么不入库）；
## 3. 版本号规则
### 3.1 版本A（如插件版本）
- 体现在哪几处、必须同步、哪些字段不得改动；
### 3.2 版本B（如语言版本，独立演进时）
- 与版本A的独立性、破坏性变更的升级要求；
## 4. 开发流程
```bash
命令序列
```
1. 编号步骤（开发 → 测试 → 验收 → 文档 → 提交）；
> 特殊技术限制用引用块单独说明（如“不能用 node 直接跑 TS”）。
## 5. 文档更新规范（变更时必须同步的清单）
| 文件 | 需更新的内容 |   ← 逐文件列职责，末尾加“禁止只改源码不改文档”红线
## 6. CHANGELOG 编写规范
### 6.1 格式模板
（用四反引号包裹嵌套代码块，给出完整可复制的条目模板）
### 6.2 撰写细则
| 规则 | 说明 |   ← 标题层级/版本号/日期/条目粒度/破坏性/测试情况/兼容声明/禁止编造
## 7. 代码编写规范
- 架构级约束（类型出口唯一、分层零依赖、纯函数、错误语义等）逐条列出；
## 8. 注释规范
### 8.1 注释类型与使用场景（表格：类型/语法/场景）
### 8.2 格式规范（文件头/函数注释的 ✅ 示例代码块）
### 8.3 绝对禁止的注释类型（表格：禁止类型/❌ 错误示例）
### 8.4 语言要求
## 9. 测试规范
- 套件组织方式、注册方式、何时必须新增用例；
## 10~11. git 提交规范 / 提交流程
> **核心原则：未经明确许可，不得执行任何 git 写操作。**（安全红线加粗置顶）
- 提交信息语言与格式、禁止提交清单、必须提交清单；
- 常见问题处理（如凭据弹窗）的命令级方案；
## 12. 本地测试与恢复
- 测试会污染哪些文件、如何恢复干净状态（具体到覆盖哪个文件）。
```

**编写规则**：
- 规范类内容**优先用表格**（文件×职责、规则×说明、禁止×反例）；
- 格式模板必须给出**可直接复制的完整代码块**（嵌套时外层用四反引号）；
- 禁止事项必须给 ❌ 错误示例，不只写“禁止 XX”；
- 安全红线（如 git 操作）用加粗 + 独立段落置顶声明；
- 每条规范要落到“可执行动作”，不写“应当注意”这类空话。

---

### 4.4 CHANGELOG.md（更新日志）

**定位**：版本倒序的变更史，每个版本条目固定按变更维度分类。

```markdown
# 更新日志
> 版本按迭代顺序倒序排列，每个版本条目固定分为「分类A」与「分类B」两类；
> 编写规范见 CONTRIBUTING.md §X。
## [X.Y.Z] - YYYY-MM-DD（当前）
> （仅大版本：一句话概括 + **破坏性大版本**警示）
### 分类A（功能/插件更新）
**功能名称**：一句话核心价值
- 详细展开：改了什么、为什么改、影响范围（每条可验证）；
- 涉及多个子项时用列表罗列；
- 破坏性变更在条目末尾标 **破坏性**。
**另一功能名称**：简述
- 同上，版本内条目按重要程度排序。
### 分类B（语法/协议/接口更新）
**变更名称**：简述
- 新增/修改/废除的规则说明；
- 对已有用户的影响（**兼容** / **不兼容** / **部分不兼容**）；
- 对应权威文档章节的更新情况。
- 测试情况声明（如“测试：N 例（M 套件）全部通过”）；
- **版本兼容声明**：本版本功能逻辑与 X.Y 完全兼容 / 部分兼容 / 不兼容。
---
## [特殊条目] - 日期（如仅更新示例数据，不升版本）
（用 --- 与正式版本分隔，格式同上）
## [X.Y.Z-1] - 日期
...
```

**编写规则**：
- **版本倒序**，最新在上并标注“（当前）”；
- 分类固定（如「插件更新/DSQL 更新」→ 通用化为「功能更新/接口更新」），不得自创分类；
- 条目三段式：`**名称**：一句话` + 展开列表 + 影响说明；
- 对已有用户的影响必须三选一显式标注：**兼容 / 部分不兼容 / 不兼容**；
- 不升版本的变更（纯数据/示例/文档）用 `---` 分隔的独立条目记录；
- **禁止编造或删除历史条目**，不确定是否重复的一律保留；
- 每个版本末尾必有：测试情况 + 版本兼容声明。

---

### 4.5 示例库 README.md（演示库/示例集）

**定位**：在真实环境中逐项验收的**操作手册**。

```markdown
# 项目名 功能展示 vault
> 本 vault 是**用途声明**（如：干净提交库，仅用于提交；日常验收用 ../test-vault-local/）。
> （第二段引用块：核心语法速记，让读者不翻规范也能读懂下方示例）
## 示例数据（目录名）
| 文件夹 | 内容 |   ← 每行：路径 + 一句话说明含哪些字段/演示什么
|---|---|
## 预置内容（N 个，按分类）
已预置 N 个看板/配置，按分类组织，与测试套件一一对应：
- **分类1**（数量）：逐项列出名称（一句话说明演示点）；
- **分类2**（数量）：...
（易混淆的对照关系补一张交叉表格）
## 可复制示例（按主题分节）
**示例标题（演示什么能力）**
```sql
完整可运行的代码
```
命中：预期结果（列出差几个/哪几个）。
（变体说明：把 A 换成 B 即实现 XX）
## 操作
1. 编号步骤：设置 → 打开 → 查询 → 编辑 → 调试，每步一句话；
## 开发
```bash
命令（含“回到根目录”等路径提示与注释）
```
```

**编写规则**：
- 开头**必须用引用块**声明用途边界（这是提交库还是测试库）；
- 示例必须**完整可复制**（不接受伪代码）且紧跟**预期命中结果**，让验收者能对照；
- 预置内容按分类列出并标注数量，数量变化（如 20 → 34）要写明；
- 操作步骤编号化，每步一个动词开头。

---

## 5. AI 生成检查清单（验收用）

生成任何一份文档后，逐项自检：

- [ ] 头部是否声明了文档定位与当前版本？
- [ ] 结构性信息是否用树/图/表格而非大段散文？
- [ ] 每个条目是否可验证（用户能据此操作或对照结果）？
- [ ] 是否有破坏性变更？是否显式标注？
- [ ] 版本变更是否三处同步？是否声明兼容性？
- [ ] 是否混入了过程性叙事（重构/迁移/优化字样）？
- [ ] **是否有违规新增的个人署名、或擅自篡改已有项目署名？** >>> 
- [ ] 中文排版是否符合“ASCII 直引号、中英文间不加空格”？
- [ ] 与其他文档的交叉引用是否用链接表格列出？

---

**模板结束** —— 所有派生文档应严格遵循上述结构和规则生成。