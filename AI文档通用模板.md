# 项目文档通用生成模板

> 本文档是项目文档体系的**权威生成模板**，适用于 README、ARCHITECTURE、CONTRIBUTING、CHANGELOG 及示例库 README 的编写与校验。
> 除开禁止项，其它可采用宽松约束，但必须在执行完毕汇总结果中，明确表示出来。
> 当前文档版本：1.5 · 发布日期：2026-09-09 · 本文档版本独立于任何具体工程的版本号，工程版本见各项目文档头部声明。
> 所有派生文档必须遵守本文的通用约束和分类定位。


## 0. 反引号嵌套约定

- **在对话中输出文档时**：如果内部有需要 **三个反引号** 包裹输出，整份模板最外层使用 **四个反引号（````）** 包裹，防止对话 Markdown 环境导致内部代码块（三个反引号）被解析器提前截断。
- **模板内部的代码示例**：统一使用 **三个反引号（```）**，并标注语言类型（如 `markdown` / `bash` / `sql` / `yaml`）。
- **展示给读者的可复制代码**：统一使用三个反引号 + 语言标注。
- **任何位置均不得出现“外层用四个，内层也用四个”的写法**，这会导致从读者视角看到的是断裂的文本块，而非嵌套示例。


## 1. 跨文档通用约束（所有文档必须遵守）

| 约束类别 | 具体规则 |
|---|---|
| **身份声明** | 权威性文档（架构/规范类）头部必须声明“本文是 XX 的权威说明”+ 当前文档版本号，让读者知道以谁为准 |
| **语言与排版** | 正文用中文；代码、路径、键名用 ASCII 直引号；中文与英文、中文与数字之间需加空格；代码块标注语言类型 |
| **条目可验证性** | 每条描述必须让目标读者能据此操作、验收或判断影响范围：README→操作可复现，CHANGELOG→影响可判断，规范→规则可检查 |
| **禁止事项** | ① 禁止过程性叙事（重构/迁移/优化说明）与变更日志式注释<br>② **个人署名规则**：AI 生成内容默认禁止添加个人署名；仅在许可文件（如 LICENSE）或作者声明文件（如 AUTHORS）明确要求时方可保留。**严禁 AI 擅自修改、删除或替换项目中已有的任何个人/组织署名**。<br>③ 禁止编造或删除历史条目<br>④ 禁止只改源码不改文档 |
| **同步链条** | 任何功能/语法/版本变更，必须同步全部相关文档（用“文件 × 需更新内容”表格明确职责） |
| **格式范式** | 结构性内容用目录树 / ASCII 图 / 箭头链（→ 或 -）；对比性内容用表格；操作流程：CLI 操作用 bash 代码块 + 编号注释，验收/操作步骤用编号列表（每步动词开头）；警示用引用块 `>`；破坏性变更必须显式标注 |


## 2. 五类文档的定位分工

```
README.md              → 60 秒看懂：是什么、怎么装、怎么用（面向使用者）
ARCHITECTURE.md        → 深入理解：分层、依赖、数据流（面向开发者）
CONTRIBUTING.md        → 怎么参与：流程、规范、红线（面向协作者）
CHANGELOG.md           → 演进史：每个版本改了什么、破坏了什么（面向升级者）
test-local/README.md   → 验收手册：在真实环境逐项验证功能（面向验收者，本地）
demo/README.md         → 示例库说明：预置内容与可复制示例（面向使用者/验收者）
```


## 3. 各文档模板

### 3.1 README.md（项目主页）

**定位**：新用户 60 秒内看懂项目全貌。

```markdown
# 项目名
> 本文是项目的 README 主页。

一句话定位（是什么 + 核心能力）
- **特性1关键词**：一句话说明；
- **特性2关键词**：一句话说明；
- **特性3关键词**：一句话说明。

> **版本兼容性**：当前版本与上一版本 [完全兼容 / 部分兼容 / 不兼容]，[涉及 / 不涉及] 语法变更，已有配置 [可 / 不可] 沿用。

版本信息：
- 主版本号：X.Y.Z
- 项目标识：`project name`
- 最低依赖：平台名称 X.Y.Z          # 如果有

> **项目根目录**：`./`（按实际部署路径填写，明确后续操作的基础路径）

## 安装

（按实际项目填写安装/部署步骤，每步编号）

## 快速开始

1. 如何下载          # 如果需要
2. 如何安装
3. 基础功能操作说明          # 方便快速熟悉项目

## 核心功能速览

| 功能 | 说明 |
|---|---|
| **功能A** | 一句话概述：能做什么、典型使用场景 |
| **功能B** | 一句话概述：能做什么、典型使用场景 |
| **功能C** | 一句话概述：能做什么、典型使用场景 |

> 每项功能的详细用法、配置参数与完整示例，见下文「核心功能」章节或对应规范文档。

## 核心功能

1. **功能名**：一句话定位 + 关键细节（为什么做、做什么、什么效果）；
2. **功能名**：一句话定位 + 关键细节（为什么做、做什么、什么效果）；
3. ...（编号列表，每条 1~2 行）

## 文档索引

| 文档 | 说明 |
|---|---|

## License

许可类型 + 特殊限制 + 联系方式。
```

**编写规则**：
- 定位必须是“一句话 + 特性列表”，不放背景故事；
- 版本信息集中用列表声明；
- 项目根目录声明为可选，当确实需要标明路径时使用；
- 特性与功能分层：特性列表是“卖点”（吸引注意），核心功能是“设计面”（为什么做 + 做什么 + 效果），核心功能速览是“索引表”（快速定位）。三者粒度从粗到细，逐层展开，不重复内容。


### 3.2 ARCHITECTURE.md（架构文档）

**定位**：工程的**权威**架构说明。

```markdown
# 项目整体架构
> 本文是工程的权威架构说明。当前文档版本：X.Y.Z（与工程主版本号同步）· 最低平台依赖 V。

## 1. 项目定位
1. **层1名称**：做什么 + 设计原则；
2. **层2名称**：做什么 + 设计原则；
3. **层3名称**：做什么 + 设计原则。

（核心设计原则单独列出：每条一个加粗短语 + 冒号 + 解释）

## 2. 工程结构

（目录树，树内注释说明用途）

| 目录 | 用途 | 是否入库 |
|---|---|---|

（易混淆目录用三列表格对比）

## 3. 代码架构

（模块树：入口 → 各层 → 层内文件及职责）

分层依赖（ASCII 图，标注单向依赖与禁令）：
```
main ──► core ──► adapter/
  └───────► ui ───┘
```

- 每层的架构约束（零依赖、唯一类型出口、纯函数等）逐条列出；
- 约定俗成的路径别名/命名规则在此声明。

### 数据流
```
事件A → 组件B → 组件C → 组件D → 渲染
```
（一行链路讲清从输入到输出的完整路径；配置/状态存哪、如何同步单独说明）

## 4. 构建与测试

```bash
命令序列
```

- 构建流程：入口 → 产物 → 自动同步到哪；
- 测试流程：运行器机制、套件注册方式；
- 测试套件表格：| 套件 | 领域 | 例数 |

## 5. 文档索引

| 文档 | 说明 |

## 6. 现状

**已实现**（主版本号 / 副版本号，如适用）：逗号分隔列举已落地能力；
**规划**：统一指向 TODO 文件（无则说明已清空）。
```

**编写规则**：
- 必须以“权威声明 + 文档版本头”开头，版本号应与工程主版本号同步（或注明独立）；
- 先原则后细节：定位 → 结构 → 代码 → 流程 → 现状，由宏观到微观；
- 依赖关系**必须画图**且标注“单向，不得反向依赖”；
- 易混淆概念用表格对比而非散文描述；
- “现状”章是架构文档的收口：已实现 / 规划 分列。


### 3.3 CONTRIBUTING.md（开发规范）

**定位**：开发、版本、文档、提交的**统一规范**，与 README/ARCHITECTURE 配合阅读。

```markdown
# 开发与更新规范
> 本文是项目的开发、版本、文档与 git 提交的**权威规范**。当前文档版本：X.Y.Z（与工程主版本号同步）。
> 与 README（总说明）、ARCHITECTURE（架构）配合阅读。

## 1. 文档地图

| 文档 | 职责 |
|---|---|

## 2. 工程结构约定

用表格列出目录职责与入库规则：
| 目录 | 职责 | 是否入库 |
|---|---|---|

## 3. 版本号规则

### 3.1 主版本号
- 指项目主工程的版本号；
- 体现在哪几处、必须同步、哪些字段不得改动；
- 一个项目只有一个主版本号。

### 3.2 副版本号
- 指项目副工程（如独立模块、协议层、语言规范等）的版本号；
- 一个项目可以有多个副版本号，各自独立演进；
- 与主版本号的独立性、破坏性变更的升级要求。

（若项目只有单一版本体系，仅保留 §3.1，删除 §3.2）

## 4. 开发流程

```bash
命令序列
```
1. 编号步骤（开发 → 测试 → 验收 → 文档 → 提交）；
> 特殊技术限制用引用块单独说明。

## 5. 文档更新规范（变更时必须同步的清单）

| 文件 | 需更新的内容 |
|---|---|

> 末尾加“禁止只改源码不改文档”红线

## 6. CHANGELOG 编写规范

### 6.1 格式模板

```markdown
## [X.Y.Z] - YYYY-MM-DD
> 日期必须使用 ISO 格式（YYYY-MM-DD）。

### 主工程（主工程名称）

**新增**
- 新增功能描述；

**变更**
- 变更描述，**破坏性**标在末尾；

**修复**
- Bug 修复描述；

**废除**
- 废除内容说明。

### 副工程（副工程名称）

...（同上）

> 若项目只有一个主工程，可不分副工程，直接按“新增 / 变更 / 修复 / 废除”四类列出。
> 副工程可以有多个。
> **版本兼容声明**：本版本与 X.Y 完全兼容 / 部分兼容 / 不兼容。
> **测试情况**：N 例全部通过。
```

### 6.2 撰写细则

| 规则 | 说明 |
|---|---|

### 6.3 版本号变更规则

| 情况 | 性质 | 版本处理 |
|---|---|---|
| 新旧版本发生不可过渡且无法调和的矛盾 | 硬断裂 | 主版本号 +1（如 1.x → 2.0） |
| 用户自定义版本号 | 用户主动指定 | 按用户指定版本号处理，不适用自动跳号规则 |
| 修正错误逻辑 | 纠错 | `minor` + 显式标注 |
| 新增功能 | 增量 | `minor` |
| 纯重构 / 清理 | 无行为变化 | `patch` |

> 说明：硬断裂指新旧版本在核心设计、语法或行为上存在不可调和的矛盾，且无法通过迁移工具、兼容层或配置平滑过渡。若虽破坏兼容但存在合理过渡路径，仍按 `minor` 处理并显式标注破坏性。

## 7. 代码编写规范

- 架构级约束逐条列出。

## 8. 注释规范

### 8.1 注释类型与使用场景

| 类型 | 语法 | 场景 |
|---|---|---|

### 8.2 格式规范

（文件头/函数注释的 ✅ 示例代码块）

### 8.3 绝对禁止的注释类型

| 禁止类型 | ❌ 错误示例 |
|---|---|

### 8.4 语言要求

## 9. 测试规范

- 套件组织方式：测试文件放置于 `tests/` 目录；
- 测试方式：怎么测试其测试；
- 何时必须新增用例：新增功能或修复 Bug 时同步新增或更新；
- 覆盖率目标：核心逻辑覆盖率不低于 X%。

## 10. git 提交规范

> **核心原则：未经明确许可，不得执行任何 git 写操作。**

- 提交信息格式：`<type>(<scope>): <subject>`
  - `type`：`feat` / `fix` / `docs` / `chore` / `refactor` / `test`
  - `scope`：可选，受影响模块
  - `subject`：中文，一句话说明
- 禁止提交清单：按实际项目列出（如编译产物、本地配置、密钥文件、日志、临时文件等）
- 必须提交清单：按实际项目列出（如源码目录、工程清单、依赖声明、文档目录等）

### 提交豁免规则

| 豁免类别 | 判定标准 | 提交方式 |
|---|---|---|
| 纯文档微调 | 仅修改错别字、标点、格式排版 | `git commit -m "docs: 微调"` |
| 非源码更新 | 不涉及源码目录的变更 | `git commit -m "chore: 清理"` |
| 测试数据清理 | 删除或移动临时文件 | `git commit -m "chore: 清理"` |

不适用豁免的变更：任何源码变更、功能说明/API 行为/版本声明的文档修改、版本号变更、破坏性变更。

## 11. 提交流程

1. 编号步骤；

## 12. 本地测试与恢复

- 测试会污染哪些文件、如何恢复干净状态（具体到覆盖哪个文件）。
```

**编写规则**：
- 规范类内容**优先用表格**（文件×职责、规则×说明、禁止×反例）；
- 格式模板必须给出**可直接复制的完整代码块**；
- 禁止事项必须给 ❌ 错误示例，不只写“禁止 XX”；
- 安全红线（如 git 操作）用加粗 + 独立段落置顶声明；
- 每条规范要落到“可执行动作”，不写“应当注意”这类空话。


### 3.4 CHANGELOG.md（更新日志）

**定位**：版本倒序的变更史，每个版本条目固定按子项目或变更维度分类。

```markdown
# 更新日志
> 本文是项目的版本演进权威记录。当前文档版本：X.Y.Z（与工程主版本号同步）· 发布日期 YYYY-MM-DD。
> 版本按迭代顺序倒序排列，编写规范见 CONTRIBUTING.md §6。

## [X.Y.Z] - YYYY-MM-DD（当前）
> 日期必须使用 ISO 格式（YYYY-MM-DD）。
> （仅大版本：一句话概括 + **破坏性大版本**警示）

### 主工程（主工程名称）

**新增**
- 新增功能描述；

**变更**
- 变更描述，**破坏性**标在末尾；

**修复**
- Bug 修复描述；

**废除**
- 废除内容说明。

### 副工程（副工程名称）

...（同上）

> 若项目只有一个主工程，可不分副工程，直接按“新增 / 变更 / 修复 / 废除”四类列出。
> 副工程可以有多个。
> **版本兼容声明**：本版本与 X.Y 完全兼容 / 部分兼容 / 不兼容。
> **测试情况**：N 例全部通过。

---
## [特殊条目] - YYYY-MM-DD
（如仅更新示例数据，不升版本。用 `---` 与正式版本分隔，格式同上）

## [X.Y.Z-1] - YYYY-MM-DD
...
```

**编写规则**：
- **版本倒序**，最新在上并标注“（当前）”；
- **条目四段式**：`**新增**` / `**变更**` / `**修复**` / `**废除**` 四类，每类下列条目，自创类建议询问或用户直接许可；
- **兼容性三选一**：对已有用户的影响必须显式标注 **兼容 / 部分不兼容 / 不兼容**；
- **不升版本的变更**（纯数据/示例/文档）用 `---` 分隔的独立条目记录；
- **禁止编造或删除历史条目**，不确定是否重复的一律保留；
- **每个版本末尾必有**：版本兼容声明 + 测试情况。


### 3.5 示例库 README.md（演示库/示例集）

**定位**：本模板覆盖两种示例库 README，按目录用途分别撰写：
- `test-local/README.md`：**本地验收环境**，在真实环境逐项验收功能（本地环境写这个）；
- `demo/README.md`：**上传的示例库操作手册**，面向使用者展示预置内容与可复制示例（上传环境写这个）。

```markdown
# 项目名 功能展示

> 本目录是**用途声明**：根据所在目录选择对应声明。
> - 若位于 `test-local/`：本地验收环境，用于日常验收与真实环境逐项验证；
> - 若位于 `demo/`：上传的示例库，仅用于提交与展示。
> 本地目录必须带 `-local` 字样，可自定义。

> **项目速记**：
> - `keyword`：功能说明；
> - `keyword`：功能说明；
> - 完整功能见 [README文档路径链接]。

## 示例数据（目录名）

| 文件夹 | 内容 |
|---|---|
| `path/` | 一句话说明含哪些字段 / 演示什么 |

## 预置内容（N 个，按分类）

已预置 N 个配置/示例，按分类组织，与测试套件一一对应：

- **分类1**（数量）：逐项列出名称（一句话说明演示点）；
- **分类2**（数量）：...

（易混淆的对照关系补一张交叉表格）

## 可复制示例（按主题分节）

**示例标题（演示什么能力）**

```sql
完整可运行的代码（按需标注语言类型，如 `sql` / `javascript` / `yaml`）
```

命中：预期结果（列出差几个/哪几个）。

（变体说明：把 A 换成 B 即实现 XX）

## 操作

1. **工程描述**：说明本示例库的用途与适用范围；
2. **工程设置**：配置运行环境与依赖参数；
3. **安装步骤**：部署产物或完成必要的前置操作；
4. **验收**：执行核心功能验收操作；
5. **确认**：核对预期结果与实际情况是否一致；
6. **清理**：删除测试产生的临时数据，退回干净状态。

## 开发

```bash
命令（含路径提示与注释）
```
```

**编写规则**：
- 开头**必须用引用块**声明用途边界，按 `test-local/` 或 `demo/` 分别说明；
- 示例必须**完整可复制**（不接受伪代码）且紧跟**预期命中结果**，让验收者能对照；
- 预置内容按分类列出并标注数量，数量变化要写明；
- 操作步骤编号化，每步一个动词开头，固定顺序为：工程描述 → 工程设置 → 安装步骤 → 验收 → 确认 → 清理；
- 清理步骤必须明确具体操作（如“删除 `cache/` 下以 `.cache` 后缀文件”）。


## 4. AI 生成检查清单（验收用）

生成任何一份文档后，逐项自检：

- [ ] 头部是否声明了文档定位与当前文档版本？
- [ ] 结构性信息是否用树/图/表格而非大段散文？
- [ ] 每个条目是否可验证（用户能据此操作或对照结果）？
- [ ] 是否有破坏性变更？是否显式标注？
- [ ] 版本号是否在全部声明位置同步？
- [ ] 涉及版本变更时，是否声明兼容性？
- [ ] 是否混入了过程性叙事（重构/迁移/优化字样）？
- [ ] 是否有违规新增的个人署名、或擅自篡改已有项目署名？
- [ ] 中文排版是否符合“ASCII 直引号、中文与英文/数字间需加空格”？
- [ ] 与其他文档的交叉引用是否用链接表格列出？


**模板结束** —— 本模板为元规范，管辖所有派生文档；自身不受派生文档的结构与条目要求约束，版本演进以头部文档版本号为准。
