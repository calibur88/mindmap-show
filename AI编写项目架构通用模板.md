# 分层架构通用规范

> 本文是分层架构设计的**权威规范**，适用于插件、扩展、嵌入应用型项目，与具体语言、平台、业务无关。  
> 当前文档版本：1.0 · 发布日期：2026-09-10 · 本文档版本独立于任何具体工程。  
> 所有派生的架构文档必须遵守本文的分层约束与命名约定；本文不受派生文档的条目要求约束。  

## 1. 核心原则

**宿主在边界、逻辑在 core、渲染是纯函数、装配在入口、类型按包分出口、依赖用工具锁死、状态用快照加变更信号。**

| 原则 | 一句话定义 | 违反后果 |
|---|---|---|
| **宿主在边界** | 宿主 SDK 只在 `host/` 出现，其余层通过接口访问宿主 | 换宿主即改全项目 |
| **逻辑在 core** | 解析、计算、状态机等纯逻辑集中在 `core/` | 逻辑与平台耦合，无法单测 |
| **渲染是纯函数** | `render` 输入数据、输出展示形态，同输入必得同输出 | 渲染不可预测，无法断言 |
| **装配在入口** | `main` 只实例化与注册，业务逻辑一律下沉 | 入口膨胀成万能模块 |
| **类型按包分出口** | 每个可独立发布的包有自己的类型出口 | 跨包类型引用导致消费者编译失败 |
| **依赖用工具锁死** | 分层规则写成静态检查规则，CI 拒红 | “人脑保证”不可持续 |
| **快照加变更信号** | 状态暴露 `getSnapshot()` + `onDidChange(cb)` | 订阅回调承载数据，跨边界不可序列化 |

## 2. 分层模型

固定七层，自下而上。层名可按语言习惯取等价词，但职责边界不变：

| 层 | 职责 | 允许依赖 |
|---|---|---|
| `types` | 跨层共享的类型/契约唯一声明处 | 零 |
| `core` | 纯逻辑：解析、计算、状态机、算法 | 仅 `types` |
| `controller` | 全局状态、刷新流水线、事件总线、编排 | `core`、`types` |
| `render` | 数据 → ViewModel（展示形态），无交互与流程 | `core` 的公开类型 |
| `ui` | ViewModel → 元素、用户动作 → 事件 | `render`、`controller` 的公开接口 |
| `views` | 宿主视图壳，生命周期与依赖注入 | 任意下层 |
| `host` | 宿主 API 适配器 | `types` + 宿主 SDK |
| `main` | 装配，零业务 | 全部 |

**依赖单向向上，不得反向依赖。** 旁支 `settings`、`utils` 被上层引用，自身不引业务层。

```
main ──► views ──► ui ──► render ──► controller ──► core ──► types
  │                                                            ▲
  └────► host ─────────────────────────────────────────────────┘
```

### 2.1 render 层职责边界

“无业务判断”容易误读为“禁止任何判断”。render 允许**格式化**与**视图逻辑**，禁止**交互逻辑**与**业务流程**：

| 类别 | 定义 | 允许 | 例 |
|---|---|---|---|
| **格式化** | 数据 → 数据，形态不变 | ✅ | 日期 → 字符串、数值 → 千分位、枚举 → 本地化标签 |
| **视图逻辑** | 数据 → 展示属性（类名、显隐、排序、分组、图标名） | ✅ | `status == error → class "state-error"` |
| **交互逻辑** | 用户动作 → 事件/状态变更 | ❌ | 点击行 → 选中/跳转 |
| **业务流程** | 跨模块编排、状态机推进 | ❌ | 保存 → 校验 → 落盘 → 广播 |

**render 的输出是 ViewModel**——纯数据，含渲染所需的一切（值、类名、图标名、是否禁用、子项顺序），不含事件处理器、宿主句柄、回调。UI 层只做两件事：**ViewModel → 元素**、**用户动作 → 事件**。

### 2.2 core 的纯度与副作用

`core` 可以持有内存状态，但禁止任何外部副作用。**读也是副作用**，必须走注入：

| 项 | 允许 | 例 |
|---|---|---|
| 内存状态 | ✅ | 缓存、计数器、状态机当前态、索引数据结构 |
| 外部副作用（写） | ❌ | 写文件、发请求、写数据库、写剪贴板 |
| 外部副作用（读） | ❌ | 读文件、读网络、读环境变量 |
| DOM / 宿主对象 | ❌ | 宿主 SDK 符号、DOM 节点、窗口句柄 |
| 日志输出 | ⚠️ 通过注入的 `ILogger`，不直接输出 | — |

所有外部交互通过接口完成，接口声明在 `types`。

## 3. 宿主边界

- `host/` 是**唯一引用宿主 SDK 的合法位置**（另含入口文件与视图壳）。
- `core` / `render` / `ui` 禁止出现宿主 SDK 的任何符号、类型、常量。
- 宿主能力一律通过**接口/协议/契约**注入，契约声明在 `types`。
- 未文档化或版本不稳定的宿主 API，必须加运行时守卫。
- 宿主边界接口按能力拆分，不堆成单一大接口：

| 接口 | 职责 |
|---|---|
| `IVaultHost` / `IDataSource` | 列文件、读内容、订阅变更 |
| `IOpener` | 打开文件/链接/外链 |
| `IFrontmatterHost` | 读写结构化字段 |
| `IStorageHost` | 持久化配置与内容（带命名空间） |
| `IUiHost` / `INotifier` | 提示、状态广播 |
| `IYamlCodec` | 序列化/反序列化，由宿主实现 |

## 4. 类型出口

- 每个**可独立发布的模块**有自己的类型出口，不共享一个全局出口。
- 单包内：`types` 是唯一出口；模块私有类型可就地声明，但不得跨层导出。
- 双包（主体 + 独立子包）：子包有自己的 `types`，主体的 `types` 不得被子包引用。
- 子包公开类型**禁止引用宿主类型**——否则编译产物的类型声明里会留下跨包路径，消费者编译即失败。
- 区分 **type-only 引用**与**运行期引用**：前者不构成依赖边，静态检查时应忽略。

## 5. 依赖方向落地

光靠人脑记不住，用工具锁死。任何语言都有对应的依赖分析工具：

| 检查项 | 规则示例 |
|---|---|
| 分层方向 | 禁止 `core → settings/views/ui/host/controller/render` 之类的边 |
| 循环依赖 | CI 跑循环检测，红灯即拒 |
| 别名一致性 | 构建配置 / 运行配置 / 测试配置三处同构，缺一不可 |
| 类型出口唯一性 | 除 `types` 外，任何模块不得跨层导出类型 |

> **“我检查过无环”不是保证，“规则禁止成环”才是。**  

## 6. 状态与订阅

- 状态用 **`getSnapshot()` + `onDidChange(cb)`**，不用 `subscribe(handlers)`。
- `cb` 只通知“变了”，消费方自己去拉 snapshot。这个信号永远比“变成什么”更容易跨边界传递。
- 快照必须是**可跨边界序列化**：只用基本类型、纯数据对象、数组，不含类实例、函数、句柄、宿主对象。
- **防抖是 controller 的职责，不是数据结构的职责。** store 只做缓存 + 通知，不藏防抖逻辑。
- store 若只做缓存 + 通知，可留在 `core`；若含防抖或触发重扫，归 `controller`。

## 7. 错误与异步约定

### 7.1 错误处理

- **读路径**：返回可空值。语言有 `Option<T>` / `Maybe<T>` / `Result<T, Error>` 时优先使用；否则用 `T | null` / `T?`。禁止用异常表达“读取失败/无数据”——这是正常返回值，不是异常。
- **写路径**：以异常/错误类型向上传递，由 UI 层捕获并展示。
- 所有跨层返回的可空值与错误类型，必须在接口签名里显式声明，不靠文档。
- 日志走注入的 `ILogger`，不直接写标准输出，让宿主能接管。
- `core` 内不做写操作——写全部由 UI 触发、UI 处理失败。

### 7.2 异步约定

- **跨层调用**：默认异步，尤其涉及 I/O 的接口（读文件、写文件、网络），返回 `Promise` / `Future` / `async` 形式。
- **core 内部计算**：同步。纯计算不需要异步，异步会污染调用链、破坏可预测性。
- **render**：纯同步。渲染是纯函数，不做 I/O。
- **阻塞约束**：任何可能超过一帧的调用（大文件读、网络、加密）必须异步，避免阻塞 UI 线程。
- **并发控制**（防抖、节流、队列、取消）属于 controller 职责，不放在 core 或 render。

## 8. 事件总线归属

| 角色 | 归属 |
|---|---|
| 事件接口/枚举 | `types`，跨层契约 |
| 事件总线实现 | `controller`（或同层的 `bus/` / `events/`） |
| 事件发送方 | `ui` 发用户动作事件，`controller` 发状态变更事件 |
| 事件订阅方 | `ui`、`controller`、`views` 可订阅 |
| `core` | **不订阅、不发送**——纯逻辑不感知事件循环 |

事件 payload 必须可跨边界序列化（同 §6 快照约束）。

## 9. 装配与生命周期

- `main` 只做装配：实例化适配器 → 实例化核心 → 注册视图 → 注册命令，业务逻辑一律下沉。
- **controller 的生命周期必须覆盖整个运行期**。main 装配后可能退场（按需激活、单次初始化），controller 实例必须由 main 持有引用或挂载到应用级容器（插件宿主、DI 容器、全局注册表）。
- 视图的订阅必须在**打开时注册、关闭时成对注销**。
- 订阅返回的释放句柄在装配时统一收集，`core` 不感知宿主的生命周期 API。
- `controller` 不主动引用 `main`，只被 main 创建和持有。方向是 `main → controller`。
- **默认值只放纯数据。** 任何需要宿主/UI 参与的“默认值”都是伪装成常量的依赖注入，该走装配。

## 10. 独立子包

子包要能独立编译/构建：

- 内部引用**一律相对路径**，不用别名；
- 对外消费方用**别名/包名**指向包入口；
- 子包自带构建配置，**显式覆盖**根配置的继承项（部分构建系统的 `extends` 是整体替换，不是合并）；
- 主包构建配置显式**排除子包目录**，避免两边各查一遍；
- 提供唯一入口文件（`index.ts` / `mod.rs` / `__init__.py`），consumer 只 import 入口；
- CI 加一步独立构建验证。

**内部相对、外部别名**是包设计的标准做法——编译产物的类型声明里全是相对路径，发版无别名问题。

| 阶段 | 处理 |
|---|---|
| 单包内分层 | 独立 `tsconfig` 只为校验边界，别名可保留 |
| 准备发版 | 用后处理工具（`tsc-alias` 类）或打包器（`tsup`/`unbuild` 类）消除产物中的别名 |

## 11. 构建与测试

- 入口唯一，产物单文件或按入口分割。
- 测试镜像源码结构，与被测模块一一对应。
- 类型检查开启最严格档（空值检查、未使用变量、未使用参数、隐式任意类型等）。
- 别名/路径配置在**构建配置 / 运行配置 / 测试配置**三处同构，缺一不可。

### 11.1 测试 Mock 策略

| 层 | 策略 | 理由 |
|---|---|---|
| `core` | 零 Mock 框架，直接 `new` 实例 | 无外部依赖；用 Mock 说明混进了外部依赖，是设计问题 |
| `controller` | 用 **fake**（内存实现）替代 host 接口 | fake 实现契约、行为可控、可断言，比 spy 更接近真实 |
| `render` | 纯函数断言输出 ViewModel | 无副作用，直接对比 |
| `ui` | 宿主无关 DOM 实现（jsdom / happy-dom 类） | 不依赖真实宿主 |
| 集成测试 | 允许跨层 + 真实依赖或最小宿主模拟 | — |

> **Mock 是对“设计不干净”的补偿。** core 无需 Mock 是干净的证明，controller 用 fake 而非 Mock 是契约清晰的证明。  

## 12. 命名规则

- 模块级类型/类 PascalCase，函数/字段 camelCase，常量 UPPER_SNAKE（按语言惯例调整）。
- 接口统一前缀（`I` / `T` / 其他，按语言习惯）。
- 视图类型常量形如 `XXX_VIEW_TYPE`，与宿主注册 API 共用。
- 样式类统一前缀，状态类用 `is-active` / `state-error` 形式。

## 13. 通用目录名映射

同一职责在不同语言/生态里的常见叫法，**一个项目里同一职责只用一个名字**：

| 职责 | 常见目录名 |
|---|---|
| 源码根 | `src/` · `source/` · `lib/` · `app/` |
| 类型/契约 | `types/` · `interfaces/` · `contracts/` · `api/` |
| 纯逻辑核心 | `core/` · `domain/` · `kernel/` · `logic/` |
| 状态与编排 | `controller/` · `app/` · `services/` · `runtime/` |
| 数据 → 展示 | `render/` · `presentation/` · `view/` · `output/` |
| 控件 | `ui/` · `widgets/` · `components/` |
| 宿主视图壳 | `views/` · `windows/` · `pages/` · `screens/` |
| 宿主适配 | `host/` · `adapters/` · `platform/` · `infra/` · `drivers/` |
| 配置 | `settings/` · `config/` · `preferences/` |
| 通用工具 | `utils/` · `common/` · `shared/` · `helpers/` |
| 事件 | `events/` · `bus/` · `signals/` |
| 测试 | `tests/` · `test/` · `spec/` · `__tests__/` |
| 构建产物 | `dist/` · `build/` · `target/` · `out/` · `bin/` |
| 示例 | `examples/` · `demo/` · `samples/` |
| 文档 | `docs/` · `doc/` |
| 脚本 | `scripts/` · `tools/` · `bin/` |

## 14. AI 生成检查清单

生成或修订任何架构文档、代码骨架后，逐项自检：

- [ ] 头部是否声明了文档定位与当前文档版本？
- [ ] 分层模型是否完整覆盖七层，且每层职责用表格定义？
- [ ] 依赖方向是否画图并标注“单向，不得反向依赖”？
- [ ] 宿主 SDK 是否只出现在 `host/`、入口与视图壳？
- [ ] `core` 是否零宿主依赖、零 I/O、零 DOM？
- [ ] `render` 的输出是否为 ViewModel（纯数据，不含回调与句柄）？
- [ ] 状态是否用 `getSnapshot()` + `onDidChange(cb)` 而非 `subscribe(handlers)`？
- [ ] 快照与事件 payload 是否可跨边界序列化？
- [ ] 读路径是否返回可空值/Result，写路径是否以异常向上传递？
- [ ] 并发控制（防抖/节流/队列）是否归 controller？
- [ ] controller 的生命周期是否由 main 持有，覆盖整个运行期？
- [ ] 独立子包是否内部相对、外部别名、独立构建、CI 校验？
- [ ] 默认值是否只放纯数据，无宿主/UI 依赖？
- [ ] core 单测是否零 Mock 框架？
- [ ] 目录命名是否同一职责只用同一个名字？
- [ ] 是否混入了过程性叙事（重构/迁移/优化字样）？
- [ ] 结构性信息是否用树/图/表格而非大段散文？
- [ ] 中文排版是否符合“ASCII 直引号、中文与英文/数字间需加空格”？
- [ ] 输出前是否已递归确认围栏层次，确保外层长度大于内层且开闭一致？
- [ ] 正文引用块多行内容是否在行尾添加两个空格，确保换行显示？

---

**规范结束** —— 本文为分层架构的元规范，管辖所有派生架构文档；自身不受派生文档的结构与条目要求约束，版本演进以头部文档版本号为准。
