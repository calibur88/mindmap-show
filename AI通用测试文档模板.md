---
name: tech-debt-audit
description: >-
  通用代码技术债审查。对任何代码库做「还债式」审查——查死代码、命名语义、
  文档一致性、旧版本残留、类型逃逸、生命周期泄漏等，强制独立验证（不信任项目自带测试）。
  关键词：技术债、代码清洗、审查、还债、命名一致性、文档漂移、旧代码残留。
version: 1.2
---

# 技术债审查规范

> 本文是**技术债审查流程的权威规范**，适用于任何代码库的还债式审查。  
> 当前文档版本：1.2 · 发布日期：2026-09-15 · 版本独立于被审项目版本。  
> 审查过程与产出必须遵守本文的独立验证协议与输出格式约束。  
> 本文档仅作为规范约束，实际情形请以项目及协商为准。  

---

## 0. md 输出规范

### 0.1 代码围栏与嵌套约定

- **最外层统一用波浪线围栏**：在对话中输出整份文档时，最外层使用 `~~~`（三个或更多波浪线）包裹，避免与内部反引号代码块互相干扰。
- **内部代码块统一用反引号**：文档内部所有代码示例使用反引号围栏，并标注语言类型（如 `markdown` / `bash` / `sql` / `yaml`）。
- **嵌套时逐层加长**：若内部代码块需要再包含代码块，从最内层 3 个反引号开始，每向外一层至少 +1。例如：最内层 3 个反引号，外层 4 个，再外层 5 个。
- **波浪线与反引号分开计数**：波浪线围栏与反引号围栏互不参与嵌套计算；最外层用波浪线时，内部反引号层数不受波浪线长度影响。
- **输出前递归确认**：从最内层开始检查每一层围栏的开始、结束与长度，确认外层长度大于内层，且开闭一致、结束围栏不带语言标注。
- **禁止**：任何位置均不得出现「外层与内层围栏长度相同」或「外层短于内层」的写法；不得在最外层混用波浪线和反引号。

### 0.2 引用块换行规则

- **正文引用块行尾加两个空格**：所有正文中以 `>` 开头的引用行，若需在渲染时换行显示，必须在行尾添加两个空格。
- **多行连续引用逐行加空格**：引用块内多个连续行，每行行尾均加两个空格，确保每行独立换行，不被合并为同一段落。
- **代码块内的引用示例不加空格**：位于代码块（反引号或波浪线围栏内）的引用行属于示例内容，保持原样，不额外添加行尾空格。
- **引用块与后续内容之间保留空行**：引用块结束后，与下一段正文之间保留一个空行，避免解析粘连。

### 0.3 其它输出约定

- 正文用中文；代码、路径、键名用 ASCII 直引号；中文与英文、中文与数字之间加空格。
- 代码块必须标注语言类型；结构性内容用目录树 / ASCII 图 / 表格，避免大段散文。

---

## 1. 使用边界

| 类别 | 典型诉求 |
|---|---|
| 代码清洗 | 死代码、重复逻辑、注释掉的代码块、遗留 TODO |
| 类型加固 | 类型逃逸、宽泛字典、未收窄 `unknown`、非空断言滥用 |
| 语义对齐 | 命名与实现不一致、布尔反义、术语漂移 |
| 文档对齐 | 文档契约漂移、过时注释、README 与实现不符 |
| 版本清理 | 旧 shim 过窗口、废弃字段、重命名别名残留 |
| 结构体检 | 分层违例、跨层直读、绕过适配层 |

**不适用**（完整例外规则见 §8）：

- 新功能设计、架构重写；
- 依赖替换、构建工具更换；
- 纯风格偏好（不做主观审美批评）；
- 性能优化——仅当 O(n²) 以上退化时例外，**判定细则见 §8**。

---

## 2. 审查契约

### 2.1 审查前查询项

| 字段 | 内容 | 必填 |
|---|---|---|
| `项目` | 项目名 + 一句话定位 | ✅ |
| `技术栈` | 语言 / 框架 / 版本 / 构建 / 测试工具 | ✅ |
| `分层契约` | 各层职责 +「谁不许 import 谁」；无分层写 `N/A` | ✅ |
| `关键机制` | 必须先理解、否则会误判为 bug 的设计决策（3~7 条） | ✅ |
| `权威文档` | 可作为契约来源的文档清单（API.md / SPEC.md / README.md 等） | ✅ |
| `审查豁免` | 文档已明确「不做」「预留」「v1 范围外」的清单（见 §8） | ✅ |
| `兼容窗口` | 当前版本 / 最低兼容版本 / CHANGELOG 已声明移除的旧行为 | ✅ |
| `审查批次` | 严重等级顺序；缺省规则见 §7.2 | ⬜ |

审查项缺失 → 停下问用户，不猜、不跳步。

### 2.2 审查启动确认（进入 §4 前的最后一步）

输入填齐后，**审查员先复述、后开工**：

```text
先确认用户语言，根据用户使用语言复述四项：
  ① 分层契约（谁依赖谁、谁不许 import 谁）
  ② 关键机制（3~7 条易误判点）
  ③ 审查豁免清单（不做 / 预留 / v1 范围外）
  ④ 首批范围（第一批审哪个模块、为什么从它开始）

等用户确认 → 进入 §4
用户纠正 → 修正理解、重新复述
用户未回应 → 停在此步，不擅自开工
```

**理由**：批次制（§7.1）能防「审到一半失焦」，防不了「一开始就理解偏了」。这一步是整个审查的**方向校准**。

---

## 3. 测试红线

| 红线 | 含义 |
|---|---|
| **证据驱动** | 每个结论必须有独立证据，禁止凭阅读印象下重债 |
| **不评风格** | 命名 / 结构偏好不做主观评价；只判「名字与实现矛盾」等可证伪项 |
| **不改代码** | 审查阶段只产报告；修改在用户确认后、由用户主导 |

---

## 4. 审查维度

**固定 14 项**（A 组 10 + B 组 4）+ **C 组可扩展槽位**（模板默认提供 2 个占位示例，调用时按项目增删改）。维度引用一律用 ID（如 `A3` / `B1`），不用序号——C 组可扩展，序号会漂移。

### 4.1 A 组 · 常规债（10 项）

| ID | 维度 | 判据 |
|---|---|---|
| `A1` | 死代码 | 未使用导出、注释掉的代码块、遗留 TODO / FIXME |
| `A2` | 重复逻辑 | 同一语义多处实现、可合并的工具函数、复制粘贴分支 |
| `A3` | 类型逃逸 | `any` / `as` / `@ts-ignore` / `@ts-expect-error` / 非空断言 `!` / 宽泛字典 / 未收窄 `unknown`——逐个说明能否用类型守卫 / 泛型 / 判别联合消除 |
| `A4` | 分层违例 | 底层引入上层依赖、绕过适配层直调外部 SDK、跨层直读配置 |
| `A5` | 资源与生命周期 | 监听器 / Observer / 定时器 / 订阅是否在销毁钩子全部注销；是否重复绑定 |
| `A6` | 错误处理一致性 | 失败是否走统一告警通道；`try/catch` 是否静默吞异常；Promise rejection 是否有出口；已声明的失败语义（结果类型）是否被吞 |
| `A7` | 魔法值 | 颜色 / 间距 / 超时 / 类名 / 正则等硬编码项——是否应进常量或配置模块 |
| `A8` | 边界一致性 | 同一归一化（trim / 大小写 / 零宽字符 / 空白折叠）是否多处各写一份；可共享前缀的解析器是否重复 |
| `A9` | 测试债 | 公共导出无对应测试、warning / 错误分支未覆盖 |
| `A10` | 文档漂移 | 代码行为与权威文档不符。**只标差异，不评价哪个对。** |

### 4.2 B 组 · 语义债（4 项，隐蔽度高，易误判）

| ID | 维度 | 检查项 |
|---|---|---|
| `B1` | 命名与实现一致性 | · `get*` 有副作用 / `is*` 返回三态 / `try*` 会 throw / `validate*` 改状态<br>· 变量 / 参数名误导（`node` 实为 `nodeId`、`ref` 实为字符串）<br>· 类型名与语义漂移（解析结果类型里塞渲染期字段、告警类型混入非告警）<br>· 布尔反义（`enabled` 变量实际取 `!enabled`）<br>· 同一概念多种叫法 |
| `B2` | 文档与实现一致性 | 从权威文档抽**可执行契约**逐条对照代码：签名、返回类型、默认值、回退规则、错误分支、排序规则、超时毫秒、事件名、公开常量。产出「文档契约 × 代码实测」二维表（§6.4） |
| `B3` | 冗余注释清理 | · 注释描述的行为已随代码演进失效<br>· 注释复述代码本身<br>· 大段历史注释、被注释包住的旧实现、分隔线式废弃区<br>· 与权威文档重复且已脱节的 JSDoc |
| `B4` | 旧版本残留 | · 兼容 shim / 适配层（兼容对象已过窗口）<br>· 类型里的废弃字段（grep 确认只有定义无使用）<br>· 重命名别名（`export const oldName = newName`，grep 确认无外部调用）<br>· 旧配置项 / 旧持久化字段（默认值表里还在但无人消费）<br>· 旧样式规则（有定义无产出点，反之亦然）<br>· 旧命令 ID / 旧入口（注册了但无触发路径）<br>**判据**：不只看 CHANGELOG，还须 grep 全库（含文档 / 示例 / 测试 / 清单文件）+ 核对当前版本与兼容窗口 |

### 4.3 C 组 · 可扩展槽位（2 项，项目专属）

| ID | 维度 | 判据 |
|---|---|---|
| `C1` | `{{项目专属维度，如「双画法视觉一致性」}}` | `{{判据}}` |
| `C2` | `{{项目专属维度，如「跨语言序列化对称性」}}` | `{{判据}}` |

---

## 5. 独立验证协议

### 5.1 禁止事项

| 禁止 | 理由 |
|---|---|
| ❌ 直接跑项目自带测试，以「全绿」下结论 | 测试可能基于旧语义编写，通过 ≠ 实现正确 |
| ❌ 在项目测试目录新增 / 修改任何文件 | 污染项目状态，破坏用户环境 |
| ❌ 引用项目现有测试作为「实现如此」的证据 | 同上，独立性丧失 |
| ❌ 把类型检查通过当作语义正确 | 类型系统只覆盖类型契约，不覆盖语义契约 |

### 5.2 独立验证工程

**位置**：当前项目/.temp 下，如已占用，请询问用户测试位置

以下必须：

```text
probe-*.{ts,py,sh}   # 每条结论一个最小脚本
stubs/               # 最小依赖 stub，不引真实框架
REPORT.md
```

**清理**：先汇总工作，待用户确认需要清理后清理工作区；

### 5.3 按层选手段

| 层 | 手段 |
|---|---|
| 纯函数层 | 直接 import 源文件跑脚本；断言写成**反向断言**（先假设代码错，构造反例） |
| 渲染 / UI 层 | 最小环境 stub，只验纯逻辑部分 |
| 外部依赖层 | 手写最小接口 stub 注入 |
| 异步 / 防抖 | 假定时器或显式等待，缩短超时验证行为；**不改源码** |

### 5.4 反向验证技巧

| 场景 | 方法 |
|---|---|
| 命名一致性（`B1`） | 读名字 + 文档，写下「按名字它应该做什么」→ 构造触发输入 → 看实际行为（副作用用 stub 记录，throw 路径用 catch 验证） |
| 文档一致性（`B2`） | 从文档抽一条可执行契约 → 写 probe 喂输入 → 断言「返回值 + 副作用 + 告警」三者都符合；三者全过才算契约成立 |
| 注释过时（`B3`） | 把注释描述的旧行为写成 probe：<br>· **不通过**（实际已是新行为）→ 注释冗余，直接报<br>· **通过** → 该条行为与注释一致；**注释其余断言需逐条抽查**，不得因一条通过就默认整段注释有效 |
| 旧代码残留（`B4`） | 全库 grep 符号 → 统计引用点 → 只有定义处 1 个引用 = 疑似死代码 → 写 probe 尝试触发（不可达则确认） |

### 5.5 证据分级

| 级别 | 内容 | 效力 |
|---|---|---|
| `L1` | 静态证据：grep / 类型 / AST / 调用图 | 可单独支撑 P2；**不足以支撑 P0/P1** |
| `L2` | 独立 probe 复现：附脚本、输入、实际输出、期望输出 | **P0/P1 必须 L2** |
| `L3` | 项目现有测试通过 | **仅作意图线索，不构成证据**（不参与任何定级） |
| `[未实测]` | 环境不支持跑脚本 | 附完整脚本 + 手推预期输出，显式标注；**最高只能标 P2** |
| `[待确认]` | probe 结果与预期不符，且**无法判定**是 probe 写错还是代码错 | 附 probe + 两种解读 + 需用户提供什么；**效力参照 `[未实测]`（不支撑 P0/P1）** |

**`[待确认]` 使用约束**：

- 必须同时给出「两种解读」——解读 A：probe 假设错；解读 B：代码实现错；
- 必须写明「需用户提供什么」才能裁决（如契约条款、设计意图、历史决策）；
- 不得用 `[待确认]` 逃避判断——能通过追加 probe 或查阅权威文档解决的，不得标此项。

**`[未实测]` 触发条件**（满足任一即触发）：

1. 项目依赖无法在当前环境内搭建最小 stub；
2. 依赖手动测试项；
3. 语言运行时缺失 / 版本不匹配。

**不得滥用**：能用 stub 解决的，不允许标 `[未实测]` 逃逸。

### 5.6 probe 脚本规范

| 约束 | 内容 |
|---|---|
| 长度 | 概要说明 |
| 依赖 | 无第三方依赖 |
| 顶部注释 | 一行说明「验证哪条契约 / 反例假设」 |
| 敏感信息 | **禁止**硬编码真实路径、密钥、用户数据；用 `{{TOKEN}}` 占位符 + stub |
| 命名 | `probe-<维度ID>-<序号>.{ts,py,sh}`，如 `probe-B1-01.ts` |
| **占位符格式** | 全文统一 `{{TOKEN}}`——大写、双花括号、无空格。如 `{{PROJECT_ROOT}}` / `{{TIMESTAMP}}` / `{{MODULE_NAME}}`；不得使用 `<TOKEN>` / `$TOKEN` / `%TOKEN%` |

### 5.7 现有测试的正确用法

- **允许读取**理解意图，但结论必须绕过它；
- 现有测试与独立 probe 矛盾 → 单列 `[测试语义冲突]`，写「现有断言 X，独立实测 Y」，**交人裁决**，不自行改测试或改代码；
- 契约无测试覆盖 → 标 `[测试盲区]`，附 probe。

---

## 6. 输出格式

### 6.1 报告文件规范

| 项 | 规范 |
|---|---|
| 位置 | `当前项目/.temp/audit-{{project}}-{{timestamp}}/REPORT.md`（临时工作区内） |
| 交付方式 | 内容全文粘贴给用户；不留副本在项目内 |
| 行号格式 | 1-based，相对仓库根，`path/to/file.ext:LINE` |
| 复查 | 用户修改后，同一 `{{timestamp}}` 下追加 `REPORT-R<n>.md`，`n` 从 2 单调递增（R2 / R3 / R4 …），**不覆盖**任何历史版本 |

### 6.2 单条结论格式

```text
[级别] 文件:行 —— 一句话结论
  ID：{A1|A2|...|C2}                  # 强制：维度 ID（用 ID，不用序号）
  证据级别：L1 | L2 | [未实测] | [待确认]
  证据：
    - 静态：grep / 调用点（≤3 行）
    - probe：脚本路径 + 输入 + 实际输出 + 期望输出
  为何是债：一句话
  修法：最小改动（见 §6.5 优先级）
  风险：影响谁 / 是否需补测试
```

### 6.3 级别定义

| 级别 | 内容 |
|---|---|
| **P0 阻断** | 分层违例成灾 / 资源泄漏 / 吞异常 / 文档契约实测 ✗ 且影响用户 / 旧 shim 已过兼容窗口仍生效 / 命名与实现语义直接矛盾导致误用 |
| **P1 应修** | 重复逻辑 / 魔法值 / 命名漂移 / 注释过时 / 死代码 / 测试盲区 |
| **P2 可选** | 风格统一 / 导出收敛 / 注释精简 |

### 6.4 「文档契约 × 代码实测」二维表（B2 完成后附）

| 契约条目 | 来源文档 | 代码实测 | 判定 |
|---|---|---|---|
| `{{契约描述}}` | `{{docs/X.md §Y}}` | `{{probe 路径}}` | ✓ / ✗ / 未测 |

### 6.5 修法优先级（A3 与「不改公共 API」冲突时）

```text
1. 先判「是否真错」——命名与实现一致时，不因风格偏好改
2. 再判「是否必须改 API」——能内部修则内部修
3. 必须改 API 时 → 列「最小破坏性方案」+ 标注「待用户裁决」
```

**硬约束**：不引依赖、不跨层、不做无关重构。

### 6.6 结尾必附

| 段 | 内容 |
|---|---|
| **收敛建议** | 只改 5 处，按性价比排序，一句话理由 |
| **测试语义冲突 / 盲区清单** | 供人裁决 |
| **已裁决修订**（若有，见 §10） | 追加式记录 |

---

## 7. 批次推进

### 7.1 核心机制

如果用户指定修复，则按用户来，否则按严重等级，逐批次修复。

### 7.2 批次顺序

| 场景 | 默认顺序 |
|---|---|
| 用户指定批次 | 按用户指定顺序 |
| 用户未指定 | 按严重等级逐批次修复（P0 → P1 → P2） |

> §2 输入的 `审查批次` 字段为空时，查本表；§2 不复制本表内容。  

### 7.3 批次边界

逐批次进行，如果用户指定批次修复、完全修复或某修复项，按用户的来。

---

## 8. 审查豁免清单

文档已明确「不做 / 预留 / v1 范围外」的项**不报**。调用时由输入 `审查豁免` 填充。

### 8.1 豁免类别

| 豁免类别 | 示例（调用时替换） |
|---|---|
| 预留语法 / 符号 | `{{预留 A / B / C}}` |
| 已裁决的白名单边界 | `{{白名单 D}}` |
| 明确「v1 范围外」 | `{{v1 不做 E}}` |
| 既定兼容策略 | `{{未文档化 API 的守卫写法}}` |

### 8.2 硬约束

- 新功能建议、架构重设计、依赖替换、构建工具更换 → 一律不报；
- **性能优化例外**：仅当性能上可优化、复杂度可简化下才报，否则不报（§1 引用本行）；
- 豁免清单的项，即使真的可以改，也不在报告里出现——**不是遗漏，是尊重设计决策**。

---

## 9. 审查员自检清单（报告交付前逐项核对）

**验证方式**列含义：`外部` = 用户可独立抽查（如检查文件系统、进程、命令历史）；`自检` = 纯自查，用户只能凭报告内容判断。

| # | 检查项 | 验证方式 |
|---|---|---|
| 1 | 每条结论是否标注了证据级别（L1 / L2 / `[未实测]` / `[待确认]`）？ | 自检 |
| 2 | P0 / P1 是否都有 L2 证据？若有 `[未实测]` / `[待确认]`，是否降级到 P2？ | 自检 |
| 3 | `[未实测]` 是否真触发 §5.5 三条件之一（防逃逸）？ | 自检 |
| 4 | `[待确认]` 是否同时给出两种解读 + 需用户提供什么？ | 自检 |
| 5 | 是否在项目测试目录新增 / 修改过文件？ | **外部**（可查 `git status`） |
| 6 | 是否引用项目现有测试作为证据？ | 自检 |
| 7 | 维度引用是否用 ID（`A3` / `B1`），而非序号？§6.2 单条格式是否强制 ID 字段？ | 自检 |
| 8 | 是否区分了「语法结构错」与「设计意图」？豁免清单项是否已剔除？ | 自检 |
| 9 | `B2` 是否附「文档契约 × 代码实测」二维表？ | 自检 |
| 10 | `A3` 与「不改公共 API」冲突时，是否按 §6.5 优先级处理？ | 自检 |
| 11 | probe 脚本是否概要说明、无第三方依赖、顶部有说明注释？ | 自检 |
| 12 | probe 是否按 §5.6 命名 `probe-<ID>-<序号>`？ | **外部**（可查 `.temp/`） |
| 13 | probe 中是否有硬编码真实路径 / 密钥 / 用户数据？ | 自检 |
| 14 | 占位符是否统一为 `{{TOKEN}}`（非 `<TOKEN>` / `$TOKEN`）？ | 自检 |
| 15 | 行号格式是否为 `path/to/file.ext:LINE`（1-based，相对根）？ | 自检 |
| 16 | 结尾是否附「收敛建议 5 处」+「测试语义冲突 / 盲区清单」？ | 自检 |
| 17 | 是否核对了 §10 已裁决修订，避免重报？ | 自检 |
| 18 | 临时工作区是否在报告汇总后可清理？ | **外部**（可查 `当前项目/.temp/`） |

---

## 10. 回滚约定

审查结论被用户裁决推翻 / 修订时：

| 环节 | 规则 |
|---|---|
| **追加，不覆盖** | 在 `REPORT.md` 尾部新增「已裁决修订」段，**不修改**原结论正文 |
| **三段式** | 每条修订写：`原结论` / `用户裁决` / `理由` |
| **状态标记** | `原结论` 标 `[已修订]`；同一结论再次修订时新增一条，保留历史 |
| **不追溯** | 已被裁决的结论，不因后续批次发现新证据而自行改判——须重新提交用户裁决 |
| **跨批次同类问题** | 同一 ID 的同类问题**各自独立成条**，每条末尾标 `同 ID 另见 L<行号>`；**不合并**——保证每条独立可裁决、独立可回滚 |

**目的**：审查报告是**只追加的日志**，不是可覆盖的文档——保留裁决轨迹，供后续复查。

---

**文档结束** —— 本文为技术债审查流程的元规范，管辖全部审查产出；自身不受被审项目的结构与规则约束，版本演进以头部 `version` 为准。
