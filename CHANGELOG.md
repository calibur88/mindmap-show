# 更新日志

> 文档版本：v1.2（2026-09-09）· 插件版本：1.2.0 · 编写规范见[CONTRIBUTING.md](CONTRIBUTING.md)§6
> 版本按迭代顺序倒序排列，每个版本条目固定分为「插件更新」与「.mms语法更新」两类。

## [1.2.0] - 2026-09-09（当前）

> MINOR版本：左栏文件夹折叠状态持久化 + 右栏三卡交互（单击高亮、两段式跳转）。

### 插件更新

**左栏文件夹折叠持久化**：`MmsSettings` 新增 `collapsedFolders: string[]`（默认 `[]`），折叠状态跨重启保持
- 点击文件夹只切换子列表 `is-collapsed` class 与箭头 `▸`／`▾`，局部更新不重绘整树，滚动位置不丢
- 折叠操作走 `updateSettingsSilently`（静默更新：只改内存＋落盘，不广播、不触发整库重扫），避免点一下折叠引发全栏重渲染
- 文件夹 `rename` 时重映射折叠记录（精确路径与子路径前缀），`delete` 时清理该路径及子路径记录
- 存量 `data.json` 缺字段时与 `DEFAULT_SETTINGS` 浅合并自动补默认值，不报错
- 搜索态临时忽略折叠（等效全部展开）但不改写持久化偏好，清空搜索后恢复原折叠状态

**右栏三卡交互**：引用链／入链／出链条目统一支持单击高亮与两段式跳转
- 单击条目（排除按钮区域）切换高亮，多条目互斥，再次单击取消；面板重渲染时自动清除
- 跳转按钮两段式：**未高亮 → 跳节点**（画布选中）、**高亮后 → 跳源码行**
- 引用链：跳节点选目标节点，高亮跳本文件 `<=>`／`::` 源码行（既有行为保持）
- 入链：跳节点选**来源节点**（`IBacklink` 新增 `sourceNodeId`），高亮跳来源文件 `<=>` 源码行；同文件来源显示「本文件」，行为与跨文件一致
- 出链：跳节点选目标节点，高亮跳**本文件 `<=>` 所在源码行**（`IOutlink` 新增 `sourceLineNo`）；断链条目仍灰显但可高亮
- 桌面／移动端一致（`touch-action: manipulation`，触屏命中区放大）

**说明**：本轮曾尝试注册插件图标（manifest `icon` 字段），因仅影响插件列表介绍、不改侧边栏按钮，已回退，`manifest.json` 保持无 `icon` 字段。

### .mms语法更新

- 无语法变更。`.mms` 语法 v1 保持不变，1.2.0 与 1.0.0 完全兼容，既有文件无需任何迁移。

---

## [1.1.0] - 2026-09-09

> MINOR版本：向后兼容地补全四方向布局、三种连线样式与 `::` 节点引用，示例库从8个扩到15个分6目录。

### 插件更新

**四方向布局**：`mms_layout` 在 `LR`／`TB` 基础上新增 `BT`（下→上）与 `RL`（右→左）
- `BT`／`RL` 是 `TB`／`LR` 的主轴镜像，节点尺寸与交叉轴坐标完全一致，仅主轴翻转
- 新增 `src/core/layout/layout.test.ts`（5例）覆盖四方向主轴推进与镜像翻转

**三种连线样式**：新增 frontmatter 键 `mms_line`，取值 `line`／`curve`／`elbow`，缺省与非法值均回退 `line`
- `curve`：三次贝塞尔曲线，推力受三处约束（安全推力上限、正对直连补偿、极限陡坡增压），保证不回环与不尖刺
- `elbow`：直角折线，垂直布局走「竖→横→竖」、水平布局走「横→竖→横」
- 渲染层新增 `src/render/shared/edges.test.ts`（12例）覆盖锚点、直线／折线插值与曲线推力

**左栏文件搜索**：标签云与文件树之间加搜索栏
- 在当前标签分组内按文件名（忽略大小写）过滤并展开全部文件夹
- 「清空」只撤销关键词不切换分组，空输入不刷新不报错；切换标签分组自动清空关键词

**右栏三卡分工**：引用链／入链／出链拆分为三张独立卡，与画布选中节点双向联动
- **引用链**：跟随选中节点，列出该节点的 `<=>` 跨边（`→` 前缀）与 `::` 节点引用（`::` 前缀）合并条目，断链灰显
- **入链**：跟随选中节点，列出 `<=>` 指向该节点的来源；同文件来源显示为「本文件」，跨文件来源显示来源文件名与行号
- **出链**：文件级聚合，仅显示当前文件指向外部文件的 `<=>`，同文件引用不进此卡
- 画布选中节点 ↔ 右栏详情双向联动，跳转按钮定位到来源文件行号

**vault 事件防抖自动重扫**：`.mms` 编辑／重命名／删除后 500 毫秒防抖整库重扫
- 文件移动后旧路径 `getDoc` 未命中时，画布与右栏显示「文件已移动或索引未同步」降级提示

**设置防抖合并落盘**：`MmsPlugin.updateSettings` 内存合并 → 400 毫秒防抖落盘 → 广播重绘 → `index.refresh()`
- `IUiHost` 新增 `offStatus` 与 `getLastState`／`getLastDetail`，避免 leaf 反复开闭累积回调

**同文件标签页复用**：`opener` 同文件再次打开时复用已有标签页并跳到目标行号

**示例库重构**：从 8 个扁平文件改为按功能用例分 6 个目录共 15 个文件
- 新增 `基础语法/`、`布局方向/`、`连线样式/`、`跨边与反链/` 四个目录
- `布局方向/` 与 `连线样式/` 用「关键字-中文名」命名，打开前即知文件用的值
- `demo.test.ts` 从 15 例扩到 35 例，按目录分组覆盖全部示例

### .mms语法更新

**`::` 节点引用**：新增点对点定位语法，不建边、不画虚线、不产生入链／出链
- 写法：`:: 节点文本`（同文件）或 `:: 文件.mms::节点文本`（跨文件）
- 解析阶段 `resolved` 回填为 `true`，右栏「引用链」卡以 `::` 前缀显示并与 `<=>` 条目混排
- 新增 `INodeRef` 类型与 `IMmsNode.nodeRefs` 字段登记节点的 `::` 引用
- 对已有用户的影响：**兼容**（纯新增语法，既有文件不受影响）
- 测试：26 例 parser 套件覆盖 `<=>` 与 `::` 分流、备注分隔与断链灰显
- **版本兼容声明**：本版本与 1.0.0 完全兼容，既有 `.mms` 文件无需任何迁移

**`mms_line` frontmatter 键**：新增连线样式控制键
- 取值 `line`／`curve`／`elbow`，缺省与非法值均回退 `line` 并记 `frontmatter-fallback` 告警
- 与既有 `mms_layout` 同级，互不影响
- 对已有用户的影响：**兼容**（缺省即 1.0.0 的 `line` 行为）
- **版本兼容声明**：本版本与 1.0.0 完全兼容

---

## [1.0.0] - 2026-09-08

> 首个正式版：`.mms`语法v1与三栏工作区自本版起固定。

### 插件更新

**三栏工作区**：左栏挂Obsidian真实sidebar、中间`.mms`专属画布、右侧详情面板，一次刷新三处同步
- 左栏：标签云按标签过滤、文件树按文件夹折叠、调试信息区聚合全工程告警、状态卡提供刷新与查看详情
- 中间：`.mms`扩展名由视图类型`mind-map-view`接管，探索视图（DOM）与全景视图（SVG）一键切换
- 右侧：视图类型`mms-detail-view`展示节点正文、注释、嵌入与反链，未选中节点时给出占位提示

**解析与索引**：全库扫描→解析→跨文件引用解析→反链索引，结果只存内存
- 节点id为`父id > 自身文本`，同名节点仅同父下合并并记`duplicate-merge`
- 跳级自动补空节点并记`level-skip`，缺根自动降级并记`no-root`
- `<=> 文件名.mms::节点文本`在第二遍扫描回填，支持前向引用，目标缺失记`missing-target`

**设置项（9项）**：默认视图、调试信息开关、3个线宽、4个间距，均为纯文本输入正数
- 改动后经400毫秒防抖自动重绘并重新扫描，无需手动刷新
- 用户手动切换过探索／全景后，不再被默认视图设置覆盖

**画布视口**：鼠标拖拽与滚轮缩放、触控单指平移与双指缩放，缩放范围0.1~8

**入口与命令**：ribbon图标`git-fork`唤起文件面板；命令`mms-open-side`打开文件面板、`mms-open-detail`打开详情面板、`mms-refresh`刷新全部`.mms`

**打开源码**：右栏按钮在新leaf以markdown源码模式打开，保存仍为`.mms`后缀

### .mms语法更新

**语法v1定版**：frontmatter四个固定键＋五类正文语法
- `mms_name`／`mms_tags`／`mms_layout`／`mms_desc`，非法值安全回退（如`mms_layout: XX`回退LR并记告警）
- `#`标题节点（层级无H6上限）、`--`子节点、普通行正文、`** `注释、`<=>`跨边、`![[]]`嵌入
- 注释判定：`** 文字`（星号后至少一个空格）进`annotation`，`**文字`紧贴保留为`content`
- 嵌入按扩展名与协议分流为`image`／`mms`／`file`／`url`四类
- 对已有用户的影响：**兼容**（首个正式版，无既有文件需要迁移）
- 测试：51例（4套件）全部通过，`tsc --noEmit`零错误
- **版本兼容声明**：本版本为首个正式版，无历史版本可比对，实现与语法v1定义完全一致

---

## [设置联动] - 2026-09-09

> 不升版本的功能补充，随1.0.0一起发布。

### 插件更新

**设置变更自动生效**：改动设置后无需手动刷新
- `MmsPlugin`新增`onSettingsChange`订阅总线，`updateSettings`落盘后经400毫秒防抖广播
- 广播后各视图先按新值重绘，再触发`MmsIndex.refresh()`整库重扫，保证数据与展示一致

**左侧状态卡**：去掉「手动刷新」文案，改为刷新与查看详情两个按钮水平居中
- 新增「查看详情」按钮，右侧详情面板被关闭后可随时从左侧重新唤起

### .mms语法更新

无。
- 对已有用户的影响：**兼容**
- 测试：51例（4套件）全部通过，`tsc --noEmit`零错误
- **版本兼容声明**：本条目不含语法变更，与1.0.0完全兼容

---

## 已知限制

以下能力当前未落地，不改变既有语法与设置：

- 画布内直接编辑或删除节点（需改源码后刷新）
- 嵌入资源在画布上渲染（当前只记录到右栏）
- 虚拟滚动（节点量级小于500时暂不需要）
- 力导向与径向布局（当前仅树形布局）
